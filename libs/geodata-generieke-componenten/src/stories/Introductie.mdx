import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introductie" />

<div>
  <h1>Geolijn Generieke componenten</h1>

  <p>
    @geolijn/generieke-componenten is een Angular library die zich richt op herbruikbare componenten. De componenten
    kunnen gebruikt worden voor geografische oplossingen en bieden onder andere geo specifieke UI-elementen zoals
    kaarten, locatiezoeker en meer.
  </p>

  <h2>Hoe installeer je de package?</h2>
  <p>
    De library is beschikbaar als een npm package. Je kunt deze installeren met npm of yarn:
  </p>

  ```npm
    npm install @geolijn/generieke-componenten
  ```
</div>

