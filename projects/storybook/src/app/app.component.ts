import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  template:
    'Start is a barebone project. Start this project with `ng run storybook:storybook`',
})
export class AppComponent {}
